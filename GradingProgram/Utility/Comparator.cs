using System.IO;

namespace GradingProgram
{
    public static class ComparatorOutput
    {
        public static bool Comparator(FileInfo file1, FileInfo file2, Comparator comparator)
        {
            return true;
        }
    }
}
